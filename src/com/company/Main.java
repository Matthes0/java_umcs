package com.company;

public class Main {
//    public Segment returnPerpendicular(Segment segment, Point point){
//    double a, b, x0, y0, x1, y1, so, r;
//    a = (segment.getA().y - segment.getB().y) / (segment.getA().x - segment.getB().x);
//    b = point.y - a * point.x;
//    x0 = point.x;
//    y0 = point.y;
//    r = segment.length();
//    so = Math.sqrt(-(y0*y0)*(2*a*x0*2*b)*y0-(a*a)*(x0*x0)-2*a*b*x0);
//    }
    public static void main(String[] args) {
	// write your code here
    }
}
